<% if logged_in? %>
  <% @queries.each do |q| %>
    <h3><%= link_to q.name, query_path(q) %></h3>
  <% end %>
<% else %>
  <h3>You are not logged in!</h3>
<% end %>

<%= link_to "Log out", logout_path, method: :delete %>

<%= form_for(@category) do |c| %>
  <%= c.label "Name of the category" %>
  <%= c.text_area :name %>
  <%= c.label "Query of the category" %>
  <%= c.text_area :category_query %>
  <%= c.submit "Update"%>
<% end %>
